@model IEnumerable<API.Models.Division>

@{
    ViewData["Title"] = "Division";
}

<h1>DIVISION</h1>

<p>
    <a href="#">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a href="#">Edit</a> |
                <a href="#">Details</a> |
                <a href="#">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
